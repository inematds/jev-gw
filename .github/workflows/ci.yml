name: testes
on: [push, pull_request, workflow_dispatch]
jobs:
  unittest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.10' }
      - run: python3 -m unittest discover -s testes -v
